# 2026-08-12

- Gmail for Cursor: local `gmail-mcp-server` (not official Google Cloud MCP). Credentials in `~/.gmail-mcp/credentials.json`. OAuth token saved as `~/.gmail-mcp/token.json`.
- GCP project `noted-compass-418912`, OAuth app **Gmail MCP**, test user `ponugotimanojkumar@gmail.com`.
- Official Cursor Gmail plugin Cloud OAuth is broken (invalid redirect). Do not use it.
- After token exists, reload Cursor if `user-gmail` MCP tools time out.
